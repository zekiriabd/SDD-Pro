"""SDD_Pro admin scripts — Tech Lead opt-in tools (never invoked by pipeline).

Contains framework maintenance, validation, smoke testing, and metrics
scripts. Run manually; not referenced from agents/, commands/, or hooks.
"""
